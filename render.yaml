services:
  - type: web
    name: thesis-ocr-api
    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y \
        tesseract-ocr \
        ghostscript

    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000